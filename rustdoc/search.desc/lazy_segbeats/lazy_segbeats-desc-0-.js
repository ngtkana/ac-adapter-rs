searchState.loadedDescShard("lazy_segbeats", 0, "区間 chmin・chmax・区間加算と、区間 …\nchmin・chmax・区間加算と、区間 min・max・sum …\n<code>Segbeats</code> の要素として使える型の要件。\n加法の単位元 $0$。\n区間 $[l, r)$ を chmax する: $a_i \\gets \\max(a_i, …\n区間 $[l, r)$ を chmin する: $a_i \\gets \\min(a_i, …\nReturns the argument unchanged.\nCalls <code>U::from(self)</code>.\n番兵として使う最大値（<code>min</code> …\n番兵として使う最小値（<code>max</code> …\n$u32$ 倍を返す: $\\mathrm{self} \\times …\n配列から構築する。\n<code>RangeBounds</code> を半開区間 $[l, r)$ に変換する。\n区間 $[l, r)$ の最大値 $\\max_{i \\in [l, r)} a_i$ …\n区間 $[l, r)$ の最小値 $\\min_{i \\in [l, r)} a_i$ …\n区間 $[l, r)$ の総和 $\\sum_{i \\in [l, r)} a_i$ …\n区間 $[l, r)$ に $x$ を加算する: $a_i \\gets a_i + …")