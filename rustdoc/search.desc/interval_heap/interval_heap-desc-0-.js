searchState.loadedDescShard("interval_heap", 0, "両端優先度キュー（interval heap）\n両端優先度キュー（interval heap）。多重集合 …\nReturns the argument unchanged.\nCalls <code>U::from(self)</code>.\n空の $S$ を構築する。\n$\\max(S)$ を返す。$S$ が空なら <code>None</code>。\n$\\min(S)$ を返す。$S$ が空なら <code>None</code>。\n$\\max(S)$ を削除して返す。$S$ が空なら <code>None</code>。\n$\\min(S)$ を削除して返す。$S$ が空なら <code>None</code>。\n$x$ を挿入する（$S \\leftarrow S \\uplus {x}$）。")