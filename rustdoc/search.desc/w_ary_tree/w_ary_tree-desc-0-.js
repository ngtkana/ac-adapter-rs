searchState.loadedDescShard("w_ary_tree", 0, "$w$-ary tree による整数集合の …\n$B$-分木（$B = …\n$x \\in S$ かどうかを返す（<code>x &lt; self.len()</code> …\nReturns the argument unchanged.\n<code>bool</code> スライスから構築する。<code>slice[i]</code> が <code>true</code> …\n$x$ を $S$ に追加する（<code>x &lt; self.len()</code> …\nCalls <code>U::from(self)</code>.\n全体集合が空（$n = 0$）かどうかを返す。\n$x \\in S$ かどうかを $x = 0, 1, \\ldots, n - 1$ …\n全体集合の大きさ $n$（<code>len</code>）を返す。\n$\\mathrm{max}(S)$ を返す。なければ <code>None</code>。\n$\\mathrm{min}(S)$ を返す。なければ <code>None</code>。\n全体集合 $[0, \\mathrm{len})$、部分集合 $S = …\n$\\mathrm{max}(S \\cap (-\\infty, x))$ を返す。\n$\\mathrm{max}(S \\cap (-\\infty, x])$ …\n$x$ を $S$ から削除する（<code>x &lt; self.len()</code> …\n$\\min(S \\cap \\small] x, \\infty \\small[)$ を返す。\n$\\mathrm{min}(S \\cap \\small[x, \\infty\\small[)$ …")